fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
python-docx==1.2.0
Faker==40.36.0
phonenumbers==9.0.36
pdfplumber==0.11.9
spacy==3.8.15
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

# Test suite (`python -m pytest tests/ -v`). Harmless in a deploy, and
# keeping it here means `pip install -r requirements.txt` is the single
# setup step for running the tests too.
pytest==8.4.2
httpx==0.28.1

